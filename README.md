# Portfolio
Personal portfolio

## Resume data

`npm run sync:data` copies `index.json` from the sibling `resume-data-source` repo when that path is available locally (meta-workspace checkout). Otherwise the committed `public/resume-data/index.json` is used for dev and build.
